import "./App.css";
import HttpStateMachine from "./components/StateMachine/HttpStateMachine";

function App() {
  return (
    <div className="App">
      <header className="App-header">
        <HttpStateMachine />
      </header>
    </div>
  );
}

export default App;
